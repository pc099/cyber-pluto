# --- Pi fork (pi/pi is a git submodule as of Session 1 — see .gitmodules) ---
# The submodule itself is tracked via a gitlink + .gitmodules, not this
# ignore file. Ignore only its build output.
/pi/pi/node_modules/
/pi/pi/**/dist/

# --- TypeScript (extensions/) ---
node_modules/
dist/
build/
*.tsbuildinfo

# --- Python (services/) ---
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Secrets — never commit ---
.env
.env.*
*.key
*.pem
secrets/
credentials.json
*.ovpn
*credentials*.json

# --- Pluto runtime state / evidence ---
# The state dir is runtime data (SQLite db + WAL/shm sidecars). Ignore it at
# the repo root and anywhere else a run's cwd might land it (e.g. extensions/),
# but never the TypeScript source dir extensions/src/state/.
/state/
**/state/*.db
**/state/*.db-*
**/state/kb/
evidence/
logs/

# --- Pi runtime config/session state (project-local, created on first run) ---
/.pi/

# --- OS / editor ---
.DS_Store
*.swp
.vscode/
.idea/
