{
  "name": "@pluto/extensions",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pluto's custom Pi extension layer (vision pipeline, agent-delegation, red-lines hook, tool-call logging, Layer 2 state, recon feedback loop).",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "hello": "npm run build && node dist/hello.js",
    "test": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "typebox": "1.3.27"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "@types/node": "^24.0.0",
    "@earendil-works/pi-coding-agent": "file:../pi/pi/packages/coding-agent"
  }
}
