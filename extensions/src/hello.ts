/**
 * Session 0 scaffolding check: proves the TypeScript workspace for the Pi
 * extension layer compiles and runs. No extension logic lives here yet —
 * vision/, delegation/, red-lines/, and tool-log/ modules land in their
 * own build-plan sessions.
 */
console.log("pluto/extensions: TypeScript workspace OK");
