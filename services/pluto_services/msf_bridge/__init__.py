"""Pluto's Metasploit bridge (Architecture §2.3.2): Metasploit-first,
custom-on-demand exploitation, behind one typed interface over a pluggable RPC
transport. The bridge is a mechanism only — all gating lives upstream in the
TS tool path."""
