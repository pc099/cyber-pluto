# Pluto Build — Progress Ledger

**Resume protocol:** read this file top-to-bottom at the start of every session. It is the single source of truth for *where the build is right now*. Keep it current **as you work** — update it after every meaningful increment, not just at session end — so a usage-limit cutoff never loses more than the current small step.

## Current position
- **Phase:** POST-PLAN — the 12-session build plan is COMPLETE; now building the operator harness interface (Camp-1, CAI-style) for heavy daily use.
- **Active work:** `cyberpluto` operator launcher + console; bug-bounty rails.
- **Status:** The harness is operable end to end via `./cyberpluto`. Build plan done; Session 12 proven on a Cap-shaped local box (real HTB Cap deferred — free VPN filters this datacenter IP).
- **Next concrete action:** Re-run against **real HTB Cap** once VPN works (HTB VIP or a residential/tunnel egress — the operator plans reverse-SSH via their laptop's residential IP + `nmap -sT`). Post-plan backlog: **containerize Pluto** (runs as root on the VPS today — top hardening item, and why OS-level privesc is circular locally); wire the blind SQLi/SSRF TS validators onto the §4.7 OOB server; finish remaining vuln-class validators (XXE, deserialization, auth, privesc) + skill entries; KB beyond CISA KEV (WSTG/PentestMonkey/CAPEC + fastembed); a universal enforcing HTTP proxy for bug-bounty rate/header on bash tools (today briefed + enforced only on Pluto's own fetches).

## In flight (granular — what is being done *right now*)
- (nothing — harness interface committed in iterations; see Done)

## Done (most recent first — with commit hash)
- **Operator harness interface (post-plan, Camp-1 / CAI-style) — committed in iterations:**
  - `cf6b2e1` **environmental-pause false-positive fix** (surfaced by the first full interactive live run, which succeeded — SQLi #4 + IDOR #6 validated, `nathan` credential recovered — but then falsely auto-paused). Root causes in `extensions/src/lifecycle/`: the health watcher scanned *every* tool's output (so a skill file mentioning "captcha" and an expected "connection refused" both tripped it) and paused on a **single** hit with no operator recovery path. Fixed: the watcher now only judges target health from `bash`/`powershell` output, ignores errored calls (an expected closed port / refused test connection is not an unhealthy target), and requires `PLUTO_UNHEALTHY_THRESHOLD` (default **3**) *consecutive* unhealthy responses — a healthy response resets the streak. The pause is now file-based (`state/PAUSED`, like the kill switch) so it survives across extension realms, and the operator clears it with the new **`/resume`** console command. Extensions build clean; **32/32 tests**.
  - `90e8fcf` docs: README rewritten to a current operator guide (run `cyberpluto`, console command table, two gates, layout); `scope.example.yaml` template (real `scope.yaml` gitignored).
  - `7414d30` **bug-bounty HTTP compliance**: `extensions/src/shared/http-policy.ts` `politeFetch` injects the program traffic-ID header (`PLUTO_TRAFFIC_ID`) + rate-limits (`PLUTO_RATE_LIMIT`); all Gate 1 validator fetches route through it. Launcher `--program/--traffic-id/--rate` + a bug-bounty compliance briefing. Verified: header on every request + paced.
  - `71181bb` **operator console (`extensions/src/cockpit/`)**: live status widget + `/status /findings(drill-in) /nodes /attempts /creds /scope /kill /approve /report /pluto`, a `record_credential` tool, and **Gate 2** by construction (`/approve` is operator-only → `submissions` row naming the approver → finding `submitted`; Pluto never auto-submits). `/report` writes a Markdown finding report from Gate 1 evidence. New credentials/submissions repos + findings `markSubmitted`/`listByTarget`/`countByStatus` + node/attempt/validation list queries. Harness-verified full arc; 32/32 tests.
  - `93a4d0a` fixes on first real launch: a `set -e` trap that killed `cyberpluto` when no objective was given (command-substitution in an assignment), and a YAML colon in the `rce-command-injection` skill description that stopped it loading.
  - `6cd1193` `cyberpluto` launcher: banner + engagement summary, scope-as-a-list pinned before launch, full stack + runtime skills, lifecycle caps, `--tunnel`/`--dry-run`, engagement briefing; interactive Pi shell (tmux for persistence — no daemon).
- **Build plan (Sessions 0–12):**
- `1a0c13b` — **Session 12: end-to-end dry run on a Cap-shaped local box (+ IDOR validator).** The build-plan finale.
  - Real HTB **Cap** (10.129.90.219) was activated but is **unreachable from this VPS** — HTB's free VPN IP-filters this datacenter host (TCP 443 SYNs dropped; UDP 1337 reaches the server but TLS never completes; identical with tls-auth and tls-crypt → not a config issue). Needs HTB VIP or a residential egress; deferred. (`.ovpn` saved at `/root/htb.ovpn`, gitignored.)
  - Built `lab/cap-clone/app.mjs` — a Cap-shaped intentionally-vulnerable box (127.0.0.1): a capture dashboard with a session-scoped numeric snapshot id and `/data/<id>` that doesn't check ownership (IDOR); id 0's pcap (valid inline libpcap) carries a plaintext FTP login (the foothold). Session required (401 otherwise) so it's real broken access control.
  - Built `extensions/src/validators/idor.ts` — a new deterministic Gate 1 IDOR validator (the §4.5 class Cap needs): differential access (same identity reaches another owner's object: 200 + different substantial data) + public-resource guard (unauth must be denied). `validate_idor` tool; both-paths tested (`idor.test.ts`). **Extensions suite 32/32**.
  - **Verified live** (Haiku, full stack: recon + validators + red-lines + lifecycle + runtime skills, scope=127.0.0.1, cap=60): Pluto nmap'd (ATT&CK-tagged recon, 16-node tree), found the dashboard + `/data` IDOR, recorded a candidate, **Gate 1 VALIDATED** it (finding #5 → validated via a real `validations` row, `idor`/`passed=1`, all 4 verifiable steps green), then exfiltrated the leaked pcap and recovered the foothold creds. Ground truth: exactly 1 validated finding / 1 passed validation — no premature validated rows, no gate bypassed.
  - SSH foothold + `cap_setuid` privesc intentionally NOT run — circular on this non-containerized root VPS; not fabricated.
  - Meets Session 12's Done condition against a Cap-shaped stand-in; rerun against real Cap once VPN works. **Closes the 12-session build plan.**
- `9739947` — **Session 11: engagement lifecycle — deterministic caps, pause, stuck detection (§10.5).** Extension at `extensions/src/lifecycle/`; pure logic in `checks.ts`. Governing principle: whether to keep going is NOT the agent's call — deterministic harness checks off the state schema (same spirit as the two gates).
  - **Hard caps** — `tool_call` hook counts `attempts` for the target (persistent, engagement-wide) + elapsed since `targets.created_at`; hitting max tool-calls or max wall-clock force-stops (block + `ctx.shutdown`), not overridable. Env: `PLUTO_MAX_TOOL_CALLS` (200), `PLUTO_MAX_WALLCLOCK_S` (3600).
  - **Environmental pause** — `tool_execution_end` watcher detects target-health signals (CAPTCHA/WAF/429/account-locked/unresponsive) and auto-pauses, flagging for review. Env: `PLUTO_UNHEALTHY_THRESHOLD` (1).
  - **Stuck detection** — off the state tree, does NOT force-stop: first nudges Pluto to an untried KB/skill angle, escalates to Chaitanya only if it persists. Signals: exact repeated call, and no-new-nodes while circling a small set. Env: `PLUTO_STUCK_REPEAT` (3), `PLUTO_STUCK_WINDOW` (8), `PLUTO_STUCK_ESCALATE` (2). **Honors §6.5.2**: a novel/varied (bypass/evasion) call is PROGRESS, never a stall — even if the prior window was circling and the tree hasn't grown (caught by the bypass-is-progress test; fixed with a novel-command guard).
  - Repo additions: `attempts.countByTarget`/`recentCommandsByTarget`, `nodes.countByTarget`.
  - **Tested** (`lifecycle.test.ts`, 7): each mechanism fires when it should and not when it shouldn't. Extensions **30/30**. A no-LLM harness drove all three through the real hook off the state DB; verified live — `PLUTO_MAX_TOOL_CALLS=1` force-stopped a real Pi engagement after one tool call (logged `hard_cap_stop`, harness shut down, agent could not override).
  - Meets Session 11's Done condition: each mechanism fires correctly against a contrived scenario without the reasoning core overriding it.
- **Session 10: remaining validators + skill catalog — delivered in 3 committed parts.**
  - `8aed9de` (part 3) — **Runtime skill catalog (§6.5)** under `runtime-skills/`. Agent-Skills format (name+description frontmatter, one SKILL.md per dir), loaded with `pi --skill runtime-skills`, NOT auto-loaded in build sessions (kept outside `.pi/skills`/`.agents/skills`). Every skill carries the §6.5.2 **required bypass/evasion section** and ties confirmation to Gate 1. Authored across all five §6.5.1 categories: vulnerabilities/ (sql-injection, cross-site-scripting, rce-command-injection, path-traversal, ssrf — the built validators, +SSRF→OOB), plus one exemplar each for frameworks/ (wordpress), protocols/ (smb), platforms/ (active-directory), tooling/ (sqlmap-playbook). Remaining vuln classes (IDOR, XXE, deserialization, auth, privesc) + more category entries follow the identical template (README notes this). **Verified live**: `pi --skill runtime-skills` discovered the catalog and auto-selected `ssrf` for a blind-SSRF prompt.
  - `3f6354c` (part 2) — **OOB interaction server for blind classes (§4.7)** in Python (`services/pluto_services/oob/`). `registry.py` (thread-safe, mints unique correlation ids, records only known ids), `server.py` (threading HTTP OAST catcher: POST /register, GET /poll/<id>, ANY /c/<id> records a callback — self-hosted so data stays private), `client.py` (async `poll_until` — never blocks; a callback may arrive late), `blind.py` (`confirm_blind` — register → trigger embeds the callback URL → async poll → a correlated hit confirms). Tested end-to-end against a real local server (confirm + reject) + registry unit tests; verified live via the CLI server. Wiring specific TS blind SQLi/SSRF validators onto it is a thin follow-on.
  - `d4a86db` (part 1) — **Expanded the Gate 1 validator library** beyond SQLi: `command-injection.ts` (echo-marker signal + id/uname impact), `path-traversal.ts` (system file served from outside web root), `xss.ts` (headless Chromium `--dump-dom` confirms script EXECUTION, not reflection). Shared `report.ts` (types + probe/marker helpers) and `gate.ts` (recordAndGate — evidence → validations row → guarded promote/reject in one place). Extension now exposes `record_candidate` + per-class `validate_*` tools; `validate_sqli` reuses the glue. `lab/web-vulns/app.mjs` = vulnerable + safe control per class. Both-paths tests per class (23/23 TS). **Verified live**: all three promoted real findings to `validated` against the lab app. Also hardened `.gitignore` for a mis-cwd'd run's state dir.
- `f595e9f` — **Session 9: agent delegation — CAI-style handoff + agent-as-tool (§6.4).** Extension at `extensions/src/delegation/`. Opus per the build-plan flag.
  - **A delegation is a separate `pi` process** with its own isolated context window (Pi has no native sub-agents). `spawn.ts` spawns them; each runs `--no-session` (ephemeral) in its own extension realm; the parent sees only final stdout.
  - **Red-lines propagation is structural**: `specialistExtensions()` is the single place a sub-agent's extension set is built, and it always prepends `red-lines` + `tool-log` before any specialist tools — so no sub-agent, for any specialist, spawns without the §10.4 gate + audit log (the Session 5 contract, enforced in one place).
  - **Two CAI modes** (`index.ts`): `delegate_handoff` (full handoff — specialist owns a sub-task end-to-end, control passes to it) and `consult_specialist` (agent-as-tool — bounded consultation, lead keeps control). Each logged like any tool call (§6.4): an `attempts` row (which sub-agent, which mode), full output retained as evidence, and a `logs/delegations.jsonl` line.
  - **Tested**: `delegation.test.ts` proves propagation structurally (every specialist set includes red-lines first; unknown specialist → safe red-lined fallback with no offensive tools; spawn argv is ephemeral with `-e red-lines`). Extensions **20/20**.
  - **Verified live** (Haiku sub-agents): full handoff → exploitation specialist took validated finding #1 to a mock session via `plan_exploitation`; agent-as-tool → analyst answered a bounded question; **red-lines propagation confirmed at the sub-agent level** — a recon sub-agent's OWN red-lines blocked an out-of-scope `nmap -sV 10.10.10.10` pre-execution (logged `toolName=bash` from the sub-agent, no nmap ran). The parent's red-lines also blocks a handoff whose task names an out-of-scope host (defense in depth).
  - **Note**: a single parent invoking three sub-agents in sequence is slow (nested cold-start Pi processes) — sub-agent spawn timeout is 600s; each mode/property verified independently rather than via the flaky triple-nested run.
  - Meets Session 9's Done condition: a validated finding is handed off end-to-end to an exploitation specialist, and a bounded consultation returns via agent-as-tool without ceding control.
- `ec72543` — **Session 8: Metasploit integration — Metasploit-first, custom-on-demand (§2.3.2).** MSF bridge in Python (`services/pluto_services/msf_bridge/`), gated tool path in TS (`extensions/src/exploit/`, `msf-bridge/`).
  - **Bridge is the mechanism, one typed interface over a pluggable RPC transport** (`rpc.py`): `MockMsfRpc` (deterministic, offline — default and tested; a full MSF stack is ~1.5GB and the doctrine says never hit a live MSF in unit tests) and `HttpMsfRpc` (real msfrpcd over msgpack-RPC, lazy `msgpack` import — the swap-in via `PLUTO_MSF_BACKEND=http` + `PLUTO_MSF_HOST/PORT/TOKEN`, bridge/decision code unchanged). `bridge.py` = `find_module` / `run_module` / `generate_custom_payload`; `decide.py` = `plan_exploit` (the explicit Metasploit-first/custom-on-demand decision); `cli.py` = the process boundary the TS tool calls.
  - **Gating lives in the TS tool path, never the bridge** (`exploit/index.ts`, `plan_exploitation` tool): (1) **validated-only** — refuses any finding not `validated` (never exploit a candidate, §5.2); (2) **red-lines** — a normal tool call, so §10.4 applies; (3) **logged** as an `attempts` row with an ATT&CK tag (msf → Initial Access/T1190, custom → Execution/T1059), custom delivery code retained under `evidence/exploits/`; (4) module execution opt-in (`execute`) and human-gated outside lab/CTF; a custom delivery never auto-executes.
  - **Tested**: pytest `test_msf_bridge.py` (mock RPC) — decision both ways, module execution opens a session, no-match → None (Python total **9/9**). TS `exploit.test.ts` — validated-only guard refuses a candidate and logs no attempt. Extensions **17/17**.
  - **Verified live** (Haiku, mock backend): finding #1 (validated vsftpd 2.3.4) → module `exploit/unix/ftp/vsftpd_234_backdoor` used + executed (attempt #8, success); finding #2 (validated bespoke .php upload) → custom delivery written + retained at `evidence/exploits/attempt-7.txt` (attempt #7); finding #3 (candidate) → refused, no attempt logged. All gated + logged.
  - **Limitation surfaced**: red-lines host extraction can't tell a listener (LHOST) from a target, so an out-of-scope LHOST is blocked (correct "fail toward blocking"; operator adds the listener host to scope). Refine when `scope_notes` is formalized (Session 11).
  - Meets Session 8's Done condition: uses an existing module on a matching target and writes a custom delivery on a bespoke one, both gated and logged.
- `e5aafe7` — **Session 7: vision pipeline — headless-browser screenshots to Claude vision.** Extension at `extensions/src/vision/`; new `screenshots` evidence type in the state schema.
  - **Schema**: added the `screenshots` table (§2.3.1's new evidence type — the reference explicitly said it should be added to §4): target/node/finding refs, url, trigger (payload_submit | new_page | captcha_suspected | manual), evidence path, sha256 (exact-dup detection), dimensions, note. Typed `ScreenshotRow` + `screenshots-repo` + wired into the shared engagement repos.
  - **Capture** (`vision/capture.ts`): shells out to **Chromium's built-in `--headless --screenshot`** — deliberately NO Puppeteer/Playwright driver dependency, keeping it the scoped XBOW-style capture the reference calls for. Writes the PNG under `evidence/screenshots/`, returns sha256, parsed PNG dimensions, base64.
  - **Extension** (`vision/index.ts`): a `capture_and_view` tool the reasoning core calls at exploitation-relevant moments — screenshots the URL, records a `screenshots` row, and returns the image as vision content (`{type:"image", data, mimeType}`) so Claude SEES the rendered page, not its HTML. Being a normal tool call, it passes through the **red-lines gate** — the URL host is scope-checked, so an out-of-scope capture is blocked pre-execution with no extra wiring.
  - **Infra**: installed system `chromium` via apt (no Docker; headless `--no-sandbox --disable-gpu` works on the VPS). **System dependency, not a repo file** — a fresh environment needs `chromium` (or `$PLUTO_CHROMIUM_BIN`) on PATH.
  - **Tested** (`vision.test.ts`): screenshots repo round-trips a row; `captureScreenshot` produces a real PNG with sha256 + correct dimensions against a hermetic `file://` page (skips cleanly with no chromium). Full extensions suite **16/16**.
  - **Verified live** (Haiku 4.5, which has vision; one-shot): Claude captured `/item?id=1` and read back the exact rendered JSON, then `/item?id=1'` (reason payload_submit) and read the SQL error straight from the image, correctly concluding SQL injection — interpreting pixels, not HTML. Ground truth: 2 `screenshots` rows (distinct sha256) + 2 PNG evidence files; in-scope captures passed the scope gate.
  - Meets Session 7's Done condition: a browser-driven step produces a screenshot Claude interprets, recorded in state.
- `73f7c53` — **Session 6: seeded knowledge base + fingerprint-triggered KB query.** First Python session; the KB lives entirely in `services/` (Python), the trigger fires from the TS recon extension via a thin IPC bridge.
  - **Vector store**: Qdrant in **local (embedded) mode** (`QdrantClient(path=state/kb)`) — no server/Docker needed, same client API swaps to hosted Qdrant later. `services/pluto_services/kb/store.py` wraps it; idempotent by construction (stable record id → deterministic UUID point id, re-ingest upserts in place).
  - **Embeddings**: a deterministic, dependency-light **hashing embedder** (`embedding.py`, no model download, offline, reproducible) behind an `Embedder` Protocol — fastembed/sentence-transformers can drop in later without touching the store. Good enough for fingerprint→CVE lexical retrieval; noted as an upgrade point for sharper semantic ranking.
  - **Sources**: full **CISA KEV** ingester (`sources/cisa_kev.py`, the highest-signal source, keyed by vendor/product). WSTG/PentestMonkey/CAPEC are declared in signal order (`ingest.py` `SIGNAL_ORDER`) and skipped-with-a-log until their ingesters are registered — a **KB-expansion follow-up** (the pipeline already runs them in order once added). `records.py` = pydantic `KbRecord`/`KbHit`, `KbSource`, `KbTier` (seeded vs self-learned kept distinct).
  - **Bridge**: `extensions/src/kb-bridge/` shells out to the Python query CLI (`python -m pluto_services.kb.cli query --json`) and parses JSON — IPC only, no KB logic in TS (CLAUDE.md boundary). The KB is an **enhancement, never a gate**: any failure logs and returns no hits rather than throwing into the agent loop.
  - **Trigger**: `extensions/src/recon/` — the fingerprint write (§2.5) now queries the KB per open service and spawns high-priority (12) `KB/KEV` child nodes under the port node, capped (3) and score-thresholded (0.25) so lexical noise doesn't flood the tree. KB hits are leads, never facts — findings stay `candidate`.
  - **Tested**: pytest `services/tests/test_kb.py` — CISA KEV normalization, ingest idempotency (double-ingest = no dup), fingerprint query surfacing the right CVE, signal-order, embedder determinism — **5/5** (`cd services && .venv/bin/python -m pytest`). Extensions suite still **14/14**. A no-LLM harness confirmed the recon→bridge→node chain against the real KB.
  - **Verified live** (Haiku, one-shot): ingested the **real CISA KEV feed (1709 records)** into `state/kb`, then nmap'd 127.0.0.1 with the KB wired — Exim smtpd 4.98.2 automatically surfaced real Exim KEV CVEs (CVE-2018-6789, CVE-2010-4344) as priority-12 nodes; the reasoning core correctly treated a lexical false-hit (OpenSMTPD) as a lead to check, not a fact. Ground truth: 3 KB nodes, 3 candidate findings, 0 validated.
  - Meets Session 6's Done condition: fingerprinting a known-vulnerable service automatically surfaces relevant KB hits and creates a new high-priority node.
- `e4ac39b` — **Session 5: red-lines pre-execution check + kill switch.** Implemented on Opus per the build plan's flag for this load-bearing session. Extension at `extensions/src/red-lines/`.
  - `rules.ts` — all 7 §10.4 categories as a data-driven ruleset (DoS, out-of-scope scanning, self-propagation, false-flag/evidence-tampering, safety-of-life systems, destructive/irreversible, sensitive-data exfiltration). Pattern rules per category + a real scope check (extracts IPv4/URL hosts from the command, compares to the engagement's allowed hosts = target host + parsed `scope_notes` + `PLUTO_SCOPE_HOSTS`). Leans toward blocking by design (safety gate): a false positive routes to human approval, a false negative runs a prohibited action.
  - `check.ts` — `checkRedLines()`, a **pure, deterministic, non-LLM** decision returning a discriminated union (`{allowed:true} | {allowed:false, ruleId, category, reason}`). Pure specifically so a Session 9 sub-agent re-applies the identical gate with no shared runtime state; a rule that throws fails toward blocking.
  - `index.ts` — the enforcement hook: a **blocking `tool_call` handler** is the choke point every tool call (built-in and custom) is forced through — no code path reaches a tool without passing it, and one `-e` load covers every tool the process exposes. The sub-agent propagation contract is documented in-file (Session 9's delegation spawner must `-e red-lines` every sub-agent).
  - `kill-switch.ts` — the halt-at-any-moment human kill switch as a file sentinel: if `state/KILL_SWITCH` (or `$PLUTO_KILL_FILE`) exists, every subsequent tool call is blocked pre-execution and `ctx.shutdown()` is requested. A file so the operator can trigger it from anywhere and it works across Pi's isolated realms.
  - Tested **both paths** (`red-lines.test.ts`): allowed in-scope command passes; every category blocks; a `DELETE ... WHERE` is NOT blocked (only unbounded deletes are); scope in/out + undefined-scope edge; host extraction; and the hook blocking a red-lined call + halting on the kill switch. Full suite **14/14 green** (`cd extensions && npm test`).
  - **Verified live** (Haiku, one-shot): with scope pinned to 127.0.0.1, an attempted nmap of out-of-scope `10.10.10.10` was blocked pre-execution (logged to `logs/red-lines.jsonl`, no probe sent — immediate completion proves nmap never ran against the non-routable host); and with `state/KILL_SWITCH` engaged, an otherwise-allowed in-scope `nmap 127.0.0.1` was halted and the harness shut down cleanly. Both events in `logs/red-lines.jsonl`.
  - Meets Session 5's Done condition exactly: a deliberately red-lined action is blocked before execution; the kill switch stops an in-progress engagement cleanly.
- `69e22fe` — **Session 4: Gate 1 SQL-injection validator (deterministic promotion).** Implemented on Opus per the build plan's flag for this session.
  - `extensions/src/validators/sqli.ts` — the deterministic, non-LLM validator (Architecture §3.1/§3.5/§4.5). Pure module (no DB/fs), sends fixed baseline/attack HTTP probes, decides by string/regex only. Requires BOTH of §4.5's proof categories: a **technical signal** (DB error string on a quote-break payload, OR a boolean 1=1/1=2 response differential) AND an **impact artifact** (a UNION-injected, self-generated nonce marker reflected back). Restraint (§4.5): injects our own marker and harvests nothing real — the lab target has a real `users` table the validator never touches. Verifiable-step chaining (§3.5): baseline-reachable → error-signal → boolean-differential → marker-extraction, each an independently recorded/checked link.
  - `extensions/src/state/findings-repo.ts` — status lifecycle now enforced in ONE place: `promote(findingId, validationId)` is the only candidate→validated path and refuses unless the finding is `candidate` AND a matching `validations` row has `passed=1`; `reject()` marks a non-reproduced candidate `rejected` (kept, not deleted). Illegal transitions throw `IllegalStatusTransition` (fail loud). New `validations-repo.ts` writes the §4.4 record.
  - `extensions/src/validators/index.ts` — the Gate 1 extension. Two tools keep propose vs. confirm distinct (§4.5): `record_sqli_candidate` (files a candidate) and `validate_sqli` (runs the deterministic validator, writes evidence + validations row, drives the guarded promote/reject). The reasoning core supplies only the injection point, never the verdict. Full process-level role separation (a distinct validator sub-agent) is deferred to Session 9; here the separation is structural — the gate is code, not LLM judgment.
  - `lab/sqli-demo/app.mjs` — an intentionally-vulnerable local lab target (127.0.0.1 only, clearly marked): vulnerable `/item` (raw string concat) + safe parameterized `/safe` control, giving the validator both a real SQLi to confirm and a safe endpoint to correctly reject. Standalone runnable (`node lab/sqli-demo/app.mjs [port]`) for the live demo; the test uses its own hermetic inline copy (kept inside the TS rootDir).
  - `extensions/src/validators/sqli.test.ts` — **both** gate paths proven (required by typescript-patterns for gate logic): real in-process vulnerable/safe servers assert pass, reject, and network-failure-as-not-passed; the status guard promotes only on a passed validation and refuses every illegal transition (failed validation, cross-finding validation, already-rejected finding). `npm test` → 5/5 green.
  - DB opener now sets `busy_timeout=5000` + WAL, since two extensions hold separate handles to the same state file (Pi's isolated module realms again).
  - **Verified live** (Haiku, one-shot, ~budget-conscious): against the lab app with `PLUTO_TARGET_LABEL=sqli-lab`, the reasoning core recorded + validated `/item` → **validated** (validation #1 passed=1, all four verifiable steps green, marker `PLUTO675VFYZGSQLI` reflected) and `/safe` → **rejected** (validation #2 passed=0). Ground-truth DB confirmed: findings #1 validated / #2 rejected, 2 validations rows, complete `evidence/validations/.../{baseline,attacks}.json` files, and 4 `attempts` rows from tool-log for the tool calls.
  - Meets Session 4's Done condition exactly: "A real SQLi candidate is deterministically reproduced and promoted to validated with a complete validations record; a non-reproducible one is marked rejected."
- `a9759be` — **Session 3: recon → candidate finding, the Layer 4 feedback loop.**
  - `extensions/src/recon/` — a new extension implementing execute → observe → update-state → decide-next. `nmap-parser.ts` deterministically regex-parses nmap's plain `-sV` port-table output (not LLM-driven — turning tool output into facts is the harness's job). `service-checklist.ts` encodes §5.1's per-service table (SMB/SSH/FTP/SNMP/RDP/HTTP(S)/databases, generic fallback for anything else, e.g. the SMTP service in our own smoke test). For each open service found: one `investigate_port` node (priority 5), two `vuln_hypothesis` children — default-cred/misconfig at priority 10, CVE-path at priority 1, exactly realizing §5.1's "seeded at higher priority... so the reasoning core naturally works the cheap checks first" — and a `findings` row via `findings-repo.create()` at a deliberately modest confidence (0.5), always `candidate` by construction.
  - Registered a `list_investigation_nodes` tool (no params) so the reasoning core can consult the real priority-ordered tree to decide what to pursue next, instead of relying only on its own conversation memory — this is the "decide next" step made concrete, not just implicit in the LLM's context window.
  - **Bug found and fixed via live testing:** Pi loads each `-e` extension file in its own isolated module realm. Session 2's assumption that both extensions would share one in-process "engagement" singleton was wrong — the first live test produced *two* separate `targets` rows (one from tool-log's copy of the bootstrap, one from recon's). Fixed in `extensions/src/state/engagement.ts` by making the bootstrap idempotent through the database itself, keyed on the target label (`targets-repo.findByLabel()` / `nodes-repo.findRoot()`), rather than an in-process cache. This is also just architecturally correct regardless of the bug — Layer 2 is "per-target memory," meant to persist across many Pi invocations against the same target. Verified the fix with a harness using cache-busting dynamic imports to genuinely reproduce Pi's module isolation, then re-verified live.
  - Extracted `extensions/src/shared/bash-command.ts` (`parseBaseTool`) out of `tool-log/attack-mapping.ts` since recon needs the same bash-command parsing to recognize nmap calls.
  - **Verified live across two separate Pi invocations** (Haiku, one-shot each, ~$0.10/run per the operator's own cost check): invocation 1 ran `nmap -sV localhost` and grew a real 7-node tree with 2 candidate findings; invocation 2 — a genuinely separate process — called `list_investigation_nodes` and correctly reasoned about the tree it had never seen before (citing real node ids/priorities, correctly choosing the default-cred/misconfig nodes over the CVE-path ones), proving cross-invocation persistence actually works, not just single-process correctness. Final `state/pluto.db`: 1 target, 7 nodes, 2 candidate findings, 0 validated rows.
  - Meets Session 3's build-plan Done condition exactly: "Pluto produces a small investigation tree with at least one candidate finding and no premature validated rows."
- `6986d33` — **Session 2: Layer 2 SQLite state schema, wired to real attempts rows.**
  - `extensions/src/state/schema.ts` implements Architecture §4 exactly: `targets`, `nodes`, `findings`, `credentials`, `attempts`, `validations`, `submissions`, all six specified indexes, plus `tactic`/`technique` columns on `attempts` per §5.4 (an explicit, small addition the reference itself calls for — "costs little to add to the schema"). Uses Node's built-in `node:sqlite` (`DatabaseSync`) — no external or native dependency, and we're already on Node 24. Bumped `extensions/package.json`'s `@types/node` to `^24` to get `node:sqlite` typings (missing from the `^20` Session 0 installed).
  - `extensions/src/state/{targets,nodes,attempts,findings}-repo.ts` — the typed repository layer (typescript-patterns: no raw SQL scattered through the codebase). `findings-repo.create()` only ever produces `status: 'candidate'` — there is no way to construct any other status here; promotion to `validated` is Gate 1's job (Session 4). `credentials`/`validations`/`submissions` have schema + row types now but no repo methods yet (unused until their sessions).
  - `extensions/src/tool-log/attempts-recorder.ts` bridges every `tool_call`/`tool_execution_end` into a real `attempts` row, alongside the existing JSONL capture (both kept — JSONL is the crash-safe raw half, the DB is the structured queryable half). Bootstraps one `targets` row + one root `recon` node per Pi session at `session_start` — deliberately simple, no per-engagement reuse or real branching yet (that's Session 3). Full tool output goes to `evidence/attempts/<id>.json`, referenced from `attempts.output_ref`.
  - `extensions/src/tool-log/attack-mapping.ts` — a small, explicit bash-command → ATT&CK tactic/technique lookup (nmap, gobuster, sqlmap, hydra, ...). Unmapped/ambiguous commands (curl, non-bash Pi tools like `read`/`edit`) are left untagged (`NULL`) rather than guessed — a wrong tag would undermine the audit trail's whole point.
  - Verified twice: a synthetic harness driving the compiled extension's handlers directly (no LLM, no cost), then the real live loop — reran the Session 1 nmap-vs-localhost prompt through Pi with `claude-haiku-4-5`, and `state/pluto.db` came out with one real `targets` row, one real `nodes` row, and one real `attempts` row (`tool='nmap'`, `tactic='Reconnaissance'`, `technique='T1595'`, `output_ref` pointing at the real captured nmap output). Then manually inserted the two real fingerprinted services (OpenSSH 10.0p2, Exim smtpd 4.98.2) as candidate findings via `findings-repo` — proving the mechanism works; the autonomous parsing that would do this without a manual step is explicitly Session 3's job, not built here.
  - Meets Session 2's build-plan Done condition exactly: "a recon run populates targets, nodes, and attempts; a fingerprinted service writes a candidate row in findings." `state/pluto.db`, `evidence/`, `logs/` are all gitignored runtime data — left in place on disk as evidence of the run, not committed.
- `3e7b061` (ledger) — **Session 1 closed out: the live reasoning→shell loop is proven.** Operator added an `ANTHROPIC_API_KEY` to Pi. Ran:
  `pi --provider anthropic --model claude-haiku-4-5 -e extensions/src/tool-log/index.ts --no-session -p "Run nmap -sV against localhost and summarize the open services you find, one line each."`
  (Haiku 4.5 and `--no-session` deliberately, to keep the operator's $5 budget essentially untouched — this is a one-shot print-mode call, not an open session.)
  - Claude decided to call the `bash` tool with `nmap -sV localhost`, Pi executed it for real, and Claude's final answer correctly summarized the two real open services it found (OpenSSH 10.0p2, Exim smtpd 4.98.2) — confirming the reasoning core reads real structured tool output back, not just issues commands blind.
  - `logs/tool-invocations.jsonl` (gitignored runtime data, not committed) captured both the `tool_call` record (exact command, exact toolCallId, timestamp) and the `tool_execution_end` record (full nmap stdout, `isError: false`) for this run — the tool-log extension works end-to-end against a live agent turn, not just the synthetic harness from the prior increment.
  - This satisfies Session 1's build-plan Done condition exactly: "a single prompt results in a real command executing and its output returning to the reasoning core, fully logged."
- `4cc442d` — Session 1: fork Pi in as a submodule, add tool-invocation logging.
  - `pi/pi` is now a git submodule (not vendored) tracking `https://github.com/pc099/pi`, pinned at `b215884` — the already-cloned nested repo from Session 0 converted in place with `git submodule add`, no history rewrite needed. Working tree inside the submodule is clean — the base agent loop is genuinely unmodified.
  - Node 20.19.2 (Session 0's provisioning) is below Pi's engine requirement (`>=22.19.0` on several packages). Installed Node v24.21.0 LTS to `/usr/local` (takes PATH precedence over the apt-installed v20, which is left in place unused). `npm install --ignore-scripts` and `npm run build` then succeeded clean inside `pi/pi` — CLI at `pi/pi/packages/coding-agent/dist/bundle/cli.js`, version `0.85.1`.
  - Installed `nmap` (7.95) via apt for the shell-command smoke test — wasn't present on this environment despite the "hardened Kali VPS" framing in CLAUDE.md.
  - Added `extensions/src/tool-log/index.ts` — a Pi extension hooking `tool_call` (before execution) and `tool_execution_end` (after) to append full, structured JSONL records to `logs/tool-invocations.jsonl`, correlated by `toolCallId`. Two lines per invocation (not one on completion) so an attempt is on the record even if the process dies mid-execution. Typed against `@earendil-works/pi-coding-agent` via a `devDependency` `file:` link to the submodule (`extensions/package.json`), so it typechecks with no `any` at the tool-call boundary — `npm run build` in `extensions/` is clean. This is the Session 1 stand-in for the Architecture §4 `attempts` table; Session 2 replaces it with real SQLite writes.
  - Verified the extension loads cleanly: `pi --no-env -e extensions/src/tool-log/index.ts -p "hello"` fails only on the expected "no API key" check — no exception from the extension factory or `session_start`.
  - `logs/` and `/.pi/` (Pi's project-local runtime config, created on first real run) added to `.gitignore` as runtime state, matching the existing `state/`/`evidence/` pattern.
  - **Not yet done — this is the resume point:** no Anthropic credentials exist on this VPS (`~/.pi` had never been initialized before this session). The reasoning core has never actually issued a tool call yet; only the wiring up to that point is proven. See Resume-here note.
- `95d1170` — Session 0: repo scaffolding — split TS/Python toolchain.
  - Provisioned the toolchain on the VPS itself: installed Node v20.19.2/npm 9.2.0 and Python 3.13.5 + venv/pip via apt (none were present before this session).
  - `extensions/` — TypeScript workspace for the future Pi extension layer (vision, delegation, red-lines, tool-log). `npm install && npm run hello` builds and runs. Plain tsconfig (ES2022/NodeNext, strict), no dependency on `pi/` yet.
  - `services/` — Python layer for the future KB/embeddings/vector-store/Metasploit-bridge code. `requirements.txt` pins pydantic, python-dotenv, requests, qdrant-client, pytest/pytest-asyncio. Venv at `services/.venv` (gitignored) verified: `python -m pluto_services.hello` runs.
  - Fleshed out `.gitignore` (build artifacts, venvs, secrets patterns, Pluto runtime state, and `/pi/` itself — see next bullet) and added a top-level `README.md` describing the layout, the split stack, the two gates, and the provisioning model (read from the Kali GUI VPS Setup Guide docx).
  - The Pi fork was already cloned at `pi/pi/` (nested `.git`) before this session started. Deliberately left **untracked** (`/pi/` added to `.gitignore`) and untouched — Session 1 decides how it's brought into this repo (submodule vs. vendored copy) and forks it for real.
  - Verified `git add -A -n` staged nothing sensitive before committing (no `node_modules/`, no `.venv/`, no `pi/`, no secrets).

## Session status board
- [x] Session 0  — Repo scaffolding
- [x] Session 1  — Fork Pi, bare-bones reasoning->shell loop
- [x] Session 2  — SQLite state schema
- [x] Session 3  — Recon -> candidate finding
- [x] Session 4  — Gate 1 validator (SQLi)            [Opus]
- [x] Session 5  — Red-lines check + kill switch       [Opus]
- [x] Session 6  — Seeded KB + fingerprint query
- [x] Session 7  — Vision pipeline
- [x] Session 8  — Metasploit integration
- [x] Session 9  — Agent delegation                    [Opus]
- [x] Session 10 — Remaining validators + skill catalog
- [x] Session 11 — Engagement lifecycle
- [x] Session 12 — HackTheBox dry run  (Cap-shaped local stand-in; real Cap pending VPN)

## Open questions / blockers
- (none)

## Resume-here note (write before stopping, or when nearing a usage limit)
- **Session 11 is fully done — 11 of 12 build-plan sessions complete. ONLY Session 12 (HackTheBox dry run) remains.** Start the final session by loading `pluto-build` (+ the other skills as the box demands), then run **Session 12 — HackTheBox dry run and iterate** per `CLAUDE_CODE_BUILD_PLAN.md` §Session 12:
  - Run Pluto end-to-end against a known HTB-style box with the full stack loaded: `-e tool-log -e recon -e validators -e red-lines -e vision -e exploit -e delegation -e lifecycle --skill runtime-skills`, KB ingested (`state/kb`), scope pinned to the box (`PLUTO_SCOPE_HOSTS`, `PLUTO_TARGET_HOST`), lifecycle caps set sanely.
  - Watch: the tree grows (recon→KB nodes), Gate 1 validates a real finding, red-lines holds (out-of-scope blocked), MSF-first exploitation runs on a validated finding, the lifecycle stops cleanly. Capture gaps → next round.
  - **Done when**: Pluto solves or measurably progresses on a box end-to-end, clean audit trail, no gate bypassed.
  - **INFRA — flag to operator first**: needs a reachable HTB-style target. Real HackTheBox requires an account + OpenVPN (check `openvpn` availability, the `.ovpn` config, and that scope/red-lines allow the HTB subnet). If HTB isn't set up, a local vulnerable box (a VM/container image like Metasploitable or a downloaded HTB retired box) is the alternative — the `lab/` apps (sqli-demo, web-vulns) are class stand-ins, not a full multi-service box, so they only exercise parts of the loop. Decide the target with the operator before running.
  - **Budget note**: a full end-to-end box run is many agentic turns — the most expensive run of the build. Keep Pi on Haiku; consider setting a modest `PLUTO_MAX_TOOL_CALLS` so a runaway can't burn the whole budget.
- **Model note:** Session 12 is NOT Opus-flagged. Sonnet 5 fine for the build/orchestration. Pi runtime stays Haiku.
- **Lifecycle operational notes (Session 11):** load `-e extensions/src/lifecycle/index.ts`. Caps: `PLUTO_MAX_TOOL_CALLS` (200), `PLUTO_MAX_WALLCLOCK_S` (3600) — force-stop + shutdown. Pause: target-health signals in tool output auto-pause (`PLUTO_UNHEALTHY_THRESHOLD`=1). Stuck: `PLUTO_STUCK_REPEAT`/`_WINDOW`/`_ESCALATE`; a novel/varied call is progress (never penalizes bypass). Events → `logs/lifecycle.jsonl`. All computed off the state DB, not agent judgment.
- **Validator/skill/OOB operational notes (Session 10):**
  - Validators: `record_candidate(endpoint, vuln_class?)` + `validate_sqli` / `validate_command_injection` / `validate_path_traversal` / `validate_xss` in the `validators` extension. Each is deterministic, both-paths tested, promotes via `findings-repo.promote`. Lab: `node lab/web-vulns/app.mjs 8891` (/ping cmdi, /file traversal, /greet xss, + *-safe controls). XSS needs `chromium`.
  - OOB (§4.7): `services/pluto_services/oob/` — run `python -m pluto_services.oob.server --port 8895`; register/poll/callback API. `confirm_blind()` in `blind.py` for blind SQLi/SSRF (async poll). Not yet wired to a TS validator (thin follow-on).
  - Skills: `runtime-skills/` (5 categories), load with `pi --skill runtime-skills`; auto-selects by description. NOT auto-loaded in build sessions. Remaining vuln classes (IDOR/XXE/deserialization/auth/privesc) + more category entries follow the same template.
  - **Known cwd footgun**: run Pi with cwd = repo root so runtime state lands in `state/` (a mis-cwd'd run wrote `extensions/state/`; `.gitignore` now covers `**/state/*.db`). Prefer `cd /root/cyber-pluto && ./pi/pi/pi-test.sh ...`.
- **Delegation operational notes (Session 9):** `delegate_handoff(task, specialist, finding_id?)` and `consult_specialist(question, specialist?)`. Specialists: `exploitation`, `recon`, `analyst` (default). `PLUTO_SUBAGENT_MODEL` (default Haiku). Every sub-agent gets red-lines first (structural). Logs → `attempts` + `logs/delegations.jsonl` + `evidence/delegations/`. 600s timeout. `-e extensions/src/delegation/index.ts`.
- **MSF operational notes (Session 8):** `plan_exploitation(finding_id, mechanism?, lhost?, lport?, execute?)` in the `exploit` extension; Python bridge `services/pluto_services/msf_bridge/` (CLI `plan|run`). Backend defaults to **mock**; real msfrpcd via `PLUTO_MSF_BACKEND=http`+`PLUTO_MSF_HOST/PORT/TOKEN` (needs `msgpack`, not installed; and real Metasploit, not installed). Validated-only + red-lines + logging in the TS tool. Custom code → `evidence/exploits/`. Known gap: LHOST scope-checked as if a target — add listener host to `PLUTO_SCOPE_HOSTS`.
- **Vision (Session 7):** needs system `chromium` (installed; `$PLUTO_CHROMIUM_BIN` overrides). `capture_and_view` tool; screenshots → `evidence/screenshots/` + `screenshots` table. `-e extensions/src/vision/index.ts`.
- **KB (Session 6):** at `state/kb` (Qdrant local mode). (Re-)ingest: `cd /root/cyber-pluto && PYTHONPATH=services services/.venv/bin/python -m pluto_services.kb.cli --kb-path state/kb ingest`. recon auto-queries → priority-12 `KB/KEV` nodes. **Follow-up** (not blocking): WSTG/PentestMonkey/CAPEC ingesters + fastembed embeddings.
- Toolchain: Node **v24.21.0** in `/usr/local`. Python **3.13.5**, venv `services/.venv`. `pi/pi` submodule (built). `extensions/` needs `npm run build` after fresh clone. Tests: `cd extensions && npm test` (TS — **32**); `cd services && .venv/bin/python -m pytest` (Python — **13**). System deps installed this project: `nmap`, `chromium`. Metasploit intentionally NOT installed (mock backend).
- **Extensions inventory** (`extensions/src/`, each a separate `-e` load unless noted): `tool-log/`, `recon/` (KB-triggering), `validators/` (Gate 1: SQLi + cmdi + traversal + xss + idor), `red-lines/` (§10.4 gate + kill switch — load in EVERY run and EVERY sub-agent), `vision/` (screenshot→vision), `exploit/` (MSF-first exploitation), `delegation/` (§6.4 handoff + agent-as-tool), `kb-bridge/` + `msf-bridge/` (TS→Python IPC shims, imported not standalone), `lifecycle/` (§10.5 caps/pause/stuck), `state/`, `shared/`. Full live run: `-e tool-log -e recon -e validators -e red-lines -e vision -e exploit -e delegation -e lifecycle` (add `--skill runtime-skills` for the doctrine catalog). Python services: `kb/` (§2.5 seeded KB), `msf_bridge/` (§2.3.2), `oob/` (§4.7 blind-class OAST). Sub-agent spawn uses the built bundle CLI (`pi/pi/packages/coding-agent/dist/bundle/cli.js`), which enforces the red-lines `tool_call` block correctly (verified in Session 9).
- **Scope + kill switch:** scope allowlist = target host + `scope_notes` + `PLUTO_SCOPE_HOSTS`; out-of-scope target blocked pre-execution. Kill switch: `touch state/KILL_SWITCH` (or `$PLUTO_KILL_FILE`). Blocks log to `logs/red-lines.jsonl`.
- **Lab target for SQLi**: `node lab/sqli-demo/app.mjs 8888` (127.0.0.1 only): `/item` vulnerable, `/safe` safe control. (Kill any stray listener with `ss -tlnp | grep 8888` when done — one lingered across demos this session.)
- The Layer 2 state DB is `state/pluto.db`; the KB is `state/kb`; whole `state/` dir gitignored. Bootstrap reuses target by label — distinct `PLUTO_TARGET_LABEL` per target, or delete `state/pluto.db*` (keep `state/kb`) for a clean tree without re-ingesting the KB.
- Extension loading gotcha: **Pi loads each `-e` extension in its own isolated module realm** — module-level singletons are NOT shared across separately-loaded extensions; shared state goes through the DB (see `state/engagement.ts`).
- Pi has a working `ANTHROPIC_API_KEY` (operator-configured, not committed) — small budget ($5), one-shot Haiku ≈ $0.10, comfortable room. **Default to the cheapest model that fits**; prefer one-shot `-p --no-session`.
- Nothing is half-finished; the repo is in a clean, committed state.
